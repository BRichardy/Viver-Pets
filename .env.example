# Supabase (Project Settings → API). Use a chave anon / publishable no client.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Opcional no MVP até precisar de bypass RLS no servidor (nunca NEXT_PUBLIC_):
# SUPABASE_SERVICE_ROLE_KEY=

# IA (fase posterior; apenas servidor):
# OPENROUTER_API_KEY=
