# Supabase (Project Settings → API). URL do projeto + chave pública (anon ou publishable).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Alternativa ao nome acima (dashboard novo):
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Opcional no MVP até precisar de bypass RLS no servidor (nunca NEXT_PUBLIC_):
# SUPABASE_SERVICE_ROLE_KEY=

# IA (fase posterior; apenas servidor):
# OPENROUTER_API_KEY=
